package libbedrockpacks

// InstallAddon installs every pack contained in the add-on file at
// addonPath onto the Minecraft Bedrock server located at serverDir. It
// returns the list of packs that were installed.
func InstallAddon(addonPath, serverDir string) ([]InstalledPack, error) {
	return nil, NotImplementedErr()
}
