- Create pack.go ?
- Clarify APIs for getting `testdata` files and directories.
- Create copyAddonFixture ?
- Check usage of IsValidServerDirectory(). Consider replacing them with `IsValidServerDirectory()` which do nto return an error or `AssertValidServerDirectory()` which do not return a bool but only an error.
